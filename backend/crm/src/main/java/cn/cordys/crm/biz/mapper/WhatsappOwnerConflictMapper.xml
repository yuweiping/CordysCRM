<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.cordys.crm.biz.mapper.WhatsappOwnerConflictMapper">

    <resultMap id="BaseResultMap" type="cn.cordys.crm.biz.domain.WhatsappOwnerConflict">
        <id column="id" property="id"/>
        <result column="contact_phone" property="contactPhone"/>
        <result column="owner_phone" property="ownerPhone"/>
        <result column="conflict_owner_phone" property="conflictOwnerPhone"/>
        <result column="conflict_time" property="conflictTime"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_user" property="createUser"/>
        <result column="update_user" property="updateUser"/>
    </resultMap>


    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO biz_whatsapp_owner_conflict
        (id, contact_phone, owner_phone, conflict_owner_phone, conflict_time, status, create_time, update_time,
        create_user, update_user)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.id}, #{item.contactPhone}, #{item.ownerPhone}, #{item.conflictOwnerPhone}, #{item.conflictTime},
            #{item.status}, #{item.createTime}, #{item.updateTime}, #{item.createUser}, #{item.updateUser}
            )
        </foreach>
    </insert>

    <select id="selectByContactPhone" resultMap="BaseResultMap">
        SELECT *
        FROM biz_whatsapp_owner_conflict
        WHERE contact_phone = #{contactPhone}
        ORDER BY conflict_time DESC
    </select>

    <select id="selectByContactPhoneAndOwner" resultMap="BaseResultMap">
        SELECT *
        FROM biz_whatsapp_owner_conflict
        WHERE contact_phone = #{contactPhone}
          AND owner_phone = #{ownerPhone}
        ORDER BY conflict_time DESC
    </select>

</mapper>
